# 🔧Platinum Application Setup
The `frontend` folder houses the user application developed with **Angular 21**, part of the Platinum project. This application consumes the backend API and manages the interface, routes, components and services needed for the web experience.
# 🏗️ Installation and setup

1. Ensure Node.js **18** or higher is installed.
2. Clone the repository and enter the directory:
   ```bash
   git clone <repo-url>
   cd entregables-projecte-final-dawpi2526_platinum_src/frontend
   ```
3. Install dependencies:
   ```bash
   npm install
   ```

# 🔌 Start development environment

Run the development server:

```bash
ng serve
```

Open `http://localhost:4200/` in your browser.

## To see the details of the structure, access [README_frontend_structure.md](README_frontend_structure.md)